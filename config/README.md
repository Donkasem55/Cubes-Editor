# WELCOME TO CUBES EDITOR!
**This is an extremely lightweight and customisable code editor, although there is no settings GUI. All configurations are done from config files, and the user must configure it themselves.**

